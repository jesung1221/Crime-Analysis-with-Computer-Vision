How to run the model:
cd src
python demo.py --task track --source ../test_data/LabRobGunTestSuccess.mp4

--source: It can be the absolute path of the video file you want to process. If not provided, it will use webcam source as default.